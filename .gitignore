# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/
.open-next/

# cloudflare local dev (secrets go in dashboard env vars for production)
.dev.vars
.wrangler/

# production
/build

# misc
.DS_Store
*.pem

# cursor / cloudflare agent setup (local only — never push)
.cursor/
.agents/

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (secrets stay in .env.local — never commit or delete)
.env
.env*.local
!.env.example

# typescript
*.tsbuildinfo
next-env.d.ts
